# Certification run records

Results of real validation runs land here as `<date>-<model>.json` (the `certification-run/v0` format emitted by `scripts/validate_traversal.py`), committed via PR. Failing records get committed too — a fail is a finding, not an embarrassment. See the runbook: `docs/runbooks/tier1-traversal-validation.md`.
